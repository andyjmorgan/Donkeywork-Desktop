{
  "name": "donkeywork-desktop-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/contracts/*.test.mjs",
    "test:web": "playwright test --config tests/integration/web-console.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
