[package]
name = "donkeywork-desktop-terminal"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
nix = { version = "0.29", features = ["term", "process", "signal", "user", "fs"] }

[dev-dependencies]
tempfile = "3"
